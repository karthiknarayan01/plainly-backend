# macOS
.DS_Store

# Env
.env
.env.local

# Placeholder until stack is finalized
__pycache__/
*.pyc
.venv/
node_modules/

# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
tfplan

# Eval run output — reproducible from eval/run_eval.py, not source
eval/results/

# Page-scale eval examples derived from a copyrighted technical book — the
# earnings-statement pages ARE committed (public disclosures), these are not.
# Regenerate locally: PLAINLY_BOOK_PDF=/path/to/book.pdf python eval/build_page_set.py
eval/pages/*-scalability-*.yaml
